import { ReactNode } from 'react'

const MessageCommonItemMetaData = ({ children }: { children: ReactNode }) => {
  return <div className={'mt-2.5 flex items-center text-xs text-text3'}>{children}</div>
}

export default MessageCommonItemMetaData
